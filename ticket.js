const vhara = 800;
const age = 62;
let student = true


if ( age < 10){
    
    const fare = 0
    console.log(vhara)
}

else{
    if(student = true ){
        // 50% discount //
        const discount = vhara * 50 / 100;
        const fare = vhara - discount
        console.log(vhara)
    }
    else
    if(age >= 60){
        // 15% discount //
        const discount = vhara * 15 / 100;
        const fare = vhara - discount
        console.log(vhara)
    }
    // else{
    //     console.log(vhara)
    // }
}